name: Install shared software dependencies

inputs:
    os:
        description: "The current operating system"
        required: true
        type: string
        options:
            - amazon-linux
            - macos
            - ubuntu
            - windows
    target:
        description: "Specified target for rust toolchain, ex. x86_64-apple-darwin"
        type: string
        required: false
        default: "x86_64-unknown-linux-gnu"
        options:
            - x86_64-unknown-linux-gnu
            - aarch64-unknown-linux-gnu
            - x86_64-apple-darwin
            - aarch64-apple-darwin
            - aarch64-unknown-linux-musl
            - x86_64-unknown-linux-musl
            - x86_64-pc-windows-msvc
    engine-version:
        description: "Engine version to install"
        required: false
        type: string
    github-token:
        description: "GITHUB_TOKEN, GitHub App installation access token"
        required: true
        type: string

runs:
    using: "composite"
    steps:
        - name: Install software dependencies for macOS
          shell: bash
          if: "${{ inputs.os == 'macos' }}"
          run: |
              brew update
              brew install openssl coreutils

        - name: Install software dependencies for Ubuntu GNU
          shell: bash
          if: "${{ inputs.os == 'ubuntu' && !contains(inputs.target, 'musl')}}"
          run: |
              sudo apt update -y
              sudo apt install -y git gcc pkg-config openssl libssl-dev

        - name: Install software dependencies for MUSL
          shell: bash
          if: "${{ contains(inputs.target, 'musl') }}"
          run: |
              apk update
              wget -O - https://sh.rustup.rs | sh -s -- -y
              source "$HOME/.cargo/env"
              apk add protobuf-dev musl-dev make gcc envsubst openssl libressl-dev py3-pip

        - name: Install software dependencies for Amazon-Linux
          shell: bash
          if: "${{ inputs.os == 'amazon-linux' }}"
          run: |
              yum install -y gcc pkgconfig openssl openssl-devel which curl gettext libasan tar --allowerasing

        - name: Setup Python for Windows
          if: "${{ inputs.os == 'windows' }}"
          uses: actions/setup-python@v5
          with:
              python-version: "3.x"

        # Install Rust toolchain (non-musl only, musl installs via rustup above)
        - name: Install Rust toolchain
          if: "${{ !contains(inputs.target, 'musl') }}"
          uses: dtolnay/rust-toolchain@stable
          with:
              targets: ${{ inputs.target }}

        # Install protoc (non-musl only, musl installs via apk above)
        - name: Install protoc
          if: "${{ !contains(inputs.target, 'musl') }}"
          uses: arduino/setup-protoc@v3
          with:
              version: "25.1"
              repo-token: ${{ inputs.github-token }}

        - name: Install engine
          if: "${{ inputs.engine-version }}"
          shell: bash
          run: |
              ENGINE_VERSION="${{ inputs.engine-version }}"
              echo "Installing Valkey/Redis ${ENGINE_VERSION}..."

              if [[ "${{ inputs.os }}" == "macos" ]]; then
                  brew install valkey || brew install redis
              elif [[ "${{ inputs.os }}" == "amazon-linux" ]]; then
                  yum install -y valkey || yum install -y redis
              elif [[ "${{ inputs.os }}" == "windows" ]]; then
                  # Install the tporadowski/redis chocolatey package; it
                  # ships a `redis-server.exe` that cluster_manager.py
                  # picks up via its `redis-server` fallback. Locate
                  # the binary after install (the path moves between
                  # package versions) and prepend its dir to PATH.
                  choco install redis-64 -y --no-progress
                  # `find` finds the .exe wherever choco dropped it.
                  redis_exe=$(find /c/ProgramData/chocolatey/lib/redis-64 -iname 'redis-server.exe' -print -quit)
                  if [ -z "$redis_exe" ]; then
                      echo "::error::redis-server.exe not found after 'choco install redis-64'"
                      exit 1
                  fi
                  # GITHUB_PATH wants Windows-style paths.
                  redis_dir=$(dirname "$redis_exe" | sed 's|^/c/|C:\\|; s|/|\\|g')
                  echo "Adding $redis_dir to GITHUB_PATH"
                  echo "$redis_dir" >> "$GITHUB_PATH"
              elif [[ "${{ contains(inputs.target, 'musl') }}" == "true" ]]; then
                  apk add --no-cache valkey || apk add --no-cache redis
              else
                  sudo apt update
                  sudo apt install -y valkey || sudo apt install -y redis-server || {
                      echo "Installing from source..."
                      curl -fsSL https://github.com/valkey-io/valkey/archive/refs/tags/${ENGINE_VERSION}.tar.gz -o valkey.tar.gz
                      tar xzf valkey.tar.gz
                      cd valkey-${ENGINE_VERSION} && make -j$(nproc) && sudo make install
                  }
              fi

              # Verify - fail loudly if install silently dropped the binary.
              # Skip on Windows because the path is only on GITHUB_PATH
              # for the next step, not the current bash session.
              if [[ "${{ inputs.os }}" != "windows" ]]; then
                  if ! command -v valkey-server && ! command -v redis-server; then
                      echo "::error::install-shared-dependencies finished without valkey-server or redis-server on PATH"
                      exit 1
                  fi
              fi
